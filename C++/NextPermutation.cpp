#include <bits/stdc++.h>
using namespace std;
void solution(string s, string ans, int n, vector<string> &vec)
{
    // if (ans.length() == n)
    // {
    //     vec.push_back(ans);
    // }
    // for (int i = 0; i < s.length(); i++)
    // {

    //     char ch = s[i];
    //     string ros = s.substr(0, i) + s.substr(i + 1);
    //     solution(ros, ans + ch, n, vec);
    // }
}
int main()
{
    string l = "abc";
    vector<string> vec;
    solution(l, "", 3, vec);
}